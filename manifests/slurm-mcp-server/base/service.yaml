apiVersion: v1
kind: Service
metadata:
  name: slurm-mcp-server
  namespace: slurm
  labels:
    app: slurm-mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: mcp
  selector:
    app: slurm-mcp-server

