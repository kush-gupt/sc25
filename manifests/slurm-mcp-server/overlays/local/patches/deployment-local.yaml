apiVersion: apps/v1
kind: Deployment
metadata:
  name: slurm-mcp-server
spec:
  template:
    spec:
      containers:
      - name: slurm-mcp-server
        # Use IfNotPresent for local development (images loaded into kind)
        imagePullPolicy: IfNotPresent
        # Reduce resources
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "250m"

