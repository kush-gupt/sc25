apiVersion: v1
kind: Service
metadata:
  name: flux-mcp-server
  namespace: flux-operator
  labels:
    app: flux-mcp-server
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP
      name: mcp
  selector:
    app: flux-mcp-server

